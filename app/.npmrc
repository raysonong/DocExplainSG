# Expo SDK 54 has optional peer-dependency ranges that npm's strict resolver
# rejects (e.g. react-native-web / @expo/metro-runtime). Accepting legacy peer
# resolution is the supported workaround and keeps `npm install` / `expo
# install` working without the --legacy-peer-deps flag every time.
legacy-peer-deps=true
